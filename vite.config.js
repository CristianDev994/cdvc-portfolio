// vite.config.js
import { defineConfig } from 'vite';

// Reemplaza 'cdvc-portfolio' por EXACTAMENTE el nombre de tu repositorio
export default defineConfig({
  base: '/cdvc-portfolio/',
  // otras opciones de Vite que uses (p. ej. plugins, alias, etc.)
});
